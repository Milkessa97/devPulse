on:
  schedule:
    - cron: "0 8 * * 1"

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Generate weekly digest
        run: |
          curl -H "Authorization: Bearer ${{ secrets.SERVICE_TOKEN }}" \
          ${{ secrets.API_URL }}/digest/weekly